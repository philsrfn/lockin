# The backend. On a simulator localhost works; on a real phone use the Mac's
# LAN address, e.g. http://192.168.1.20:3000
EXPO_PUBLIC_API_URL=http://localhost:3000

# Must match APP_BEARER_TOKEN in the repo root .env
EXPO_PUBLIC_API_TOKEN=replace-me
