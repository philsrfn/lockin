# Expo SDK 57 pins react@19.2.3, but a transitive react-dom wants 19.2.8.
# The conflict is upstream and harmless here — the app has no web target.
# Without this, EAS Build resolves differently to local and `npm ci` fails.
legacy-peer-deps=true
